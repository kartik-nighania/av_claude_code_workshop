name: CD — Build, Push to ECR, Deploy to EC2
on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths: ["av_session_7_2/**"]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "stub on main — real steps live on the feature branch"
